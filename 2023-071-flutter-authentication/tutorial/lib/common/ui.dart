import 'package:flutter/material.dart';

const defaultScreenDecoration = BoxDecoration(
  gradient: LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [
      Colors.deepPurpleAccent,
      Colors.deepOrange,
    ],
  ),
);
